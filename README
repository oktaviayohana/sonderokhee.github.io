To host this app on your local machine, follow these steps:

    1) Install node.js if not done already
    2) Run the command 'npm install' to install all dependencies
    3) Create a file in the root directory named '.env' and copy (and alter) the values from '.env.example'
    4) Run 'npx vite' to host a development server
    5) Follow link printed to console

Stack used:

    - Frontend
        - React
        - sass (css)
    
    - Backend
        - Vite
        - mysql (database)
        - sequelize (interacting with database)
        - bcrypt (hashing passwords)
        - jsonwebtoken (sessions)

Notes on how this app works:
